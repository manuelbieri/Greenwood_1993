initval;
    C   = log(Cs);
    cm  = log(cms);
    ch  = log(chs);
    k   = log(ks);
    km  = log(kms);
    kh  = log(khs);
    x   = log(xs);
    xm  = log(xms);
    xh  = log(xhs);
    l   = log(ls);
    hm  = log(hms);
    hh  = log(hhs);
    zm  = log(zms);
    zh  = log(zhs);
    r   = log(rs);
    w   = log(ws); 
    y   = log(ys);
    T   = log(Ts);
    em  = 0;
    eh  = 0;
end;